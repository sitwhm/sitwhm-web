image: node:20

cache:
  paths:
    - node_modules/

deploy-pages:
  stage: deploy
  pages: true
  script:
    - npm ci
    - npm run build
    - mv out public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
